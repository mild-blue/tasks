export interface Location {
  id: string;
  // TODO: Define the rest parameters accordingly to the response object returned from API
  //  or accordingly to Swagger documentation at https://ockovani.mild.blue/swagger-ui
}
